[NEEDS]
1. refactor code, styles and layouts to minimize code debt (doublicate svgs, comments, console.log and other)
2. Check if test mode not (in BusinessPlan) not crashes main logic and rebuild fast-boot docker containers

[CHECKS]

[FEATURES]
1. Unit testing
2. e2e testing
3. add more redis cache data
4. add proxy not from package, but from code [deprecated, because of build + nginx proxy]

[BACKEND]

[BUGS]
